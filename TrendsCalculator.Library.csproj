<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <Description>Calculate Trending Models for a given input data bucketed windows period, with each window segmented into equal parts.
Models must contain these three attributes:-
1)localz(double)
2)globalz(double)
3)countWithPeriods(List&lt;int&gt;) with 
size of list =(windowPeriod+1)*number of segments of each unit
OR
size of list =(windowPeriod)*number of segments of each unit

Returns a List&lt;T&gt; containing list of trending models.</Description>
    <Version>1.0.2</Version>
    <PackageId>TrendsCalculator</PackageId>
    <PackageReleaseNotes>Initial Release</PackageReleaseNotes>
    <Authors>Microsoft Developers</Authors>
    <Company>Microsoft</Company>
    <Product>TrendsCalculator</Product>
  </PropertyGroup>

</Project>
