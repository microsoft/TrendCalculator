<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <IncludeSymbols>true</IncludeSymbols>
    <SymbolPackageFormat>snupkg</SymbolPackageFormat>
    <Description>Trending Calculator is a library for calculating the trending models out of the given list of models.The algorithm takes into account primarily two parameters for calculating the trending models out of the given list of data:-

The spike i.e., the trends indicating the picking up of a particular model.
The quantity in which the model is picked up.

Three modes for calculation:
a. ZMean criteria.
b. Custom
c. DemandSupply

Please visit the github documentation on how to consume.</Description>
    <Version>2.0.0</Version>
    <PackageId>TrendsCalculator</PackageId>
    <PackageReleaseNotes>This release contains below changes:
a. Renaming of the calculation methods.
b. Abstraction of the internal models from the output. The result will be sorted per the algorithm type without revealing internal scores.</PackageReleaseNotes>
    <Authors>Purunjay Bhal, Puneet Walecha</Authors>
    <Company>Microsoft</Company>
    <Product>TrendsCalculator</Product>
    <PackageTags>Trending, Trends, Statistics, C#, Z-Score</PackageTags>
    <PackageRequireLicenseAcceptance>false</PackageRequireLicenseAcceptance>
    <RepositoryUrl>https://github.com/microsoft/TrendCalculator</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <LangVersion>8.0</LangVersion>
  </PropertyGroup>

</Project>
